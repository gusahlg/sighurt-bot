# Build artifacts
/target/

# Environment (contains secrets)
.env

# Database files
/data/*.db
/data/*.db-journal
/data/*.db-wal
/data/*.db-shm

# Scraped message transcripts (per-channel TSVs + cursors, and DMs).
# These are private server/DM content and the training archive — they must
# NOT be pushed to a public remote. Machine-local only.
/data/channels/
/data/.scrape.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
